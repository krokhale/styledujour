= toolbar :profile, :subject => @contact.receiver_subject, :option => 'contacts' 

-# location link_to(t("contact.#{ @contact.action }.title", :name => @contact.receiver.name),edit_contact_path(@contact.to_param)) 

.row-fluid
  .span2#side-navigation
    .well.sidebar-nav
      %ul.nav.nav-list
        %li.nav-header Menu
        %li.active
          %a{:href=>"#{home_path}"}
            %i.icon-home
            Home
        %li
          %a{:href=>"#{hcit_index_path}"}
            %i.icon-play
            HCIT
  .span6#activity-feed
    .span6
      %h2= link_to(t("contact.#{ @contact.action }.title", :name => @contact.receiver.name),edit_contact_path(@contact.to_param)) 
      = render :partial => 'form' 
  .span2  
    =# yield :toolbar 